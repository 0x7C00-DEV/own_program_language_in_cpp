class A {
    x: int;
    constructor(a: int) {
        this.x = a;
    }
}

class Test {
    x: A;
    constructor(a: int) {
        this.x = new A(a);
    }
}

let k = new Test(12);
k.x.x += 1;
Println(k.x.x);